export const APP_NAME = 'CareNest';
export const TOP_BAR_HEIGHT = 64;
export const BOTTOM_NAV_HEIGHT = 80;

// Safe padding to ensure content doesn't go behind nav bars
export const SCREEN_PADDING_BOTTOM = BOTTOM_NAV_HEIGHT + 16;
export const SCREEN_PADDING_TOP = TOP_BAR_HEIGHT + 16;
