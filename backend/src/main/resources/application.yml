spring:
  application:
    name: carenest-backend

  servlet:
    multipart:
      max-file-size: 6MB
      max-request-size: 8MB

  profiles:
    active: dev

  ai:
    vertex:
      ai:
        gemini:
          enabled: false

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/carenest_db}
    username: ${SPRING_DATASOURCE_USERNAME:carenest_user}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      password: ${SPRING_DATA_REDIS_PASSWORD:${REDIS_PASSWORD:}}

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

app:
  jwt:
    secret: ${JWT_SECRET:carenest-256-bit-secret-key-for-dev-environment-only-change-in-prod}
    access-token-expiration: ${JWT_ACCESS_EXPIRATION:900000}
    refresh-token-expiration: ${JWT_REFRESH_EXPIRATION:604800000}
  features:
    ai-chat-enabled: ${APP_FEATURE_AI_CHAT_ENABLED:true}
    ocr-enabled: ${APP_FEATURE_OCR_ENABLED:true}
  ai:
    enabled: ${AI_ENABLED:true}
    provider: ${AI_PROVIDER:gemini}
    api-key: ${AI_API_KEY:}
    service-base-url: ${AI_SERVICE_BASE_URL:http://localhost:8000}
    chat-model: ${AI_MODEL_CHAT:gemini-2.5-flash}
    ocr-model: ${AI_MODEL_OCR:gemini-2.5-flash}
    timeout-ms: ${AI_TIMEOUT_MS:15000}
    max-retries: ${AI_MAX_RETRIES:2}
  ocr:
    enabled: ${OCR_ENABLED:true}
  media:
    upload-dir: ${MEDIA_UPLOAD_DIR:uploads/media}
    max-image-size-bytes: ${MEDIA_MAX_IMAGE_SIZE_BYTES:5242880}
  seed:
    qa-demo:
      enabled: ${APP_SEED_QA_DEMO_ENABLED:false}
      default-password: ${APP_SEED_QA_DEMO_DEFAULT_PASSWORD:}
      admin-email: ${APP_SEED_QA_DEMO_ADMIN_EMAIL:admin@carenest.com}
      patient-primary-email: ${APP_SEED_QA_DEMO_PATIENT_PRIMARY_EMAIL:anhkhoa.vv@gmail.com}
      patient-secondary-email: ${APP_SEED_QA_DEMO_PATIENT_SECONDARY_EMAIL:doletuankiet06@gmail.com}
      doctor-pediatric-email: ${APP_SEED_QA_DEMO_DOCTOR_PEDIATRIC_EMAIL:bacsinhikhoa@gmail.com}
      doctor-general-email: ${APP_SEED_QA_DEMO_DOCTOR_GENERAL_EMAIL:bacsidakhoa@gmail.com}
      moderator-email: ${APP_SEED_QA_DEMO_MODERATOR_EMAIL:qa.moderator@gmail.com}

      admin-full-name: ${APP_SEED_QA_DEMO_ADMIN_FULL_NAME:CareNest Admin}
      patient-primary-full-name: ${APP_SEED_QA_DEMO_PATIENT_PRIMARY_FULL_NAME:Khoa Vu}
      patient-secondary-full-name: ${APP_SEED_QA_DEMO_PATIENT_SECONDARY_FULL_NAME:Tuan Kiet}
      doctor-pediatric-full-name: ${APP_SEED_QA_DEMO_DOCTOR_PEDIATRIC_FULL_NAME:Bác sĩ Nhi Khoa}
      doctor-general-full-name: ${APP_SEED_QA_DEMO_DOCTOR_GENERAL_FULL_NAME:Bác sĩ Đa Khoa}
      moderator-full-name: ${APP_SEED_QA_DEMO_MODERATOR_FULL_NAME:QA Moderator}

server:
  port: 8080
  servlet:
    context-path: /api/v1
